Convert this project into a completely standalone production-ready Node.js application for Pterodactyl Panel deployment.

IMPORTANT:
Do NOT remove, redesign, or break any existing feature.

Keep everything working exactly as it is:
- Homepage
- Services
- Docs
- Minecraft Hosting
- VPS/RDP
- Network
- About
- DDoS page
- Reviews page
- Admin panel
- Authentication
- Database
- Images
- Uploads
- Routing
- API
- Animations
- Responsive design

The goal is only to make the project deployable on any Node.js server without Replit.

Requirements:

1. Remove every Replit-specific dependency.
- No Replit Auth
- No Replit Plugins
- No Replit Runtime packages
- No .replit dependency
- No replit-specific environment requirements

2. Keep authentication working using the existing email/password login.

3. Make the server start using only:

npm install
npm run build
npm start

4. package.json must contain production scripts:

"build": "vite build"
"start": "node server/index.js"

or equivalent production commands.

5. Express must serve:
- frontend build
- backend API
- uploaded images
- admin panel
- static assets

from one Node.js server.

6. Automatically serve the built frontend after build.

7. Ensure client-side routing works.
Refreshing pages like:

/
 /services
 /docs
 /minecraft-hosting
 /vps
 /network
 /about
 /ddos
 /reviews
 /login
 /register
 /admin

must never return 404.

Configure Express fallback correctly.

8. Database must initialize automatically.

If SQLite:
- automatically create database if missing.

If PostgreSQL:
- read DATABASE_URL from .env.

9. Upload folders must automatically be created if missing.

10. Never hardcode localhost.

Server must use:

process.env.PORT || 3000

and

0.0.0.0

11. Create a production .env.example including every required variable.

12. Admin panel must remain fully functional.

Admin login:
ipc23771@gmail.com

Password:
ipc23771

All admin features must continue working:
- Plans
- Reviews
- Homepage editing
- DDoS editing
- Docs editing
- Images
- Uploads
- Users

13. Remove every dependency on Replit Secrets.

Use normal .env variables instead.

14. Verify every API endpoint still works.

15. Verify every page still works.

16. Verify every uploaded image still loads.

17. Verify routing after refresh.

18. Verify login sessions work.

19. Verify admin permissions work.

20. Create a deployment-ready folder named:

production-build/

Inside it include ONLY the files required for deployment:

server/
dist/
public/
uploads/
package.json
package-lock.json (or pnpm-lock.yaml)
.env.example

and any required production files.

Do NOT include:
.git
.github
.replit
.replit.nix
artifacts
attached_assets
export
mockup folders
documentation generated by Replit
temporary files
node_modules

21. Create a DEPLOYMENT.md containing exact Pterodactyl instructions:

Startup:
npm start

Install:
npm install

Build:
npm run build

Node Version:
22 LTS

Environment variables required.

22. Before finishing, perform a complete self-check:

✓ npm install succeeds
✓ npm run build succeeds
✓ npm start succeeds
✓ Website loads
✓ Admin panel loads
✓ Authentication works
✓ Reviews work
✓ Plans work
✓ DDoS page works
✓ Docs work
✓ No missing modules
✓ No Replit dependency remains

Do NOT change the website design or functionality.

Only convert it into a clean, production-ready standalone Node.js application that can be deployed by uploading the ZIP to a Pterodactyl Node.js server, extracting it, running npm install, npm run build, and npm start with no manual code changes.