# BruteScale — Environment Variables
# Copy this file to .env and fill in your values before starting the server.

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production

# ── Security ──────────────────────────────────────────────────────────────────
# JWT signing secret — MUST be changed in production!
# Generate one: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING_64_CHARS_MIN

# ── Admin Account ─────────────────────────────────────────────────────────────
# On first startup the server automatically creates this account with role=admin.
# If the account already exists it is promoted to admin automatically.
ADMIN_EMAIL=ipc23771@gmail.com
ADMIN_PASSWORD=ipc23771
